# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  vmImage: ubuntu-latest

jobs:
  - job: job1
    steps:
      - script: echo "This is job1"
        displayName: 'Job1 is executed'

  - job: job1
    steps:
      - script: echo "This is job2"
        displayName: 'Job2 is executed'
  
  - job: job3
    steps:
      - script: echo "This is job3"
        displayName: 'Job 3 is executed'
